/* globals describe: false, before:false, after:false, afterEach: false*/

// globals expect:true  
//var expect = require('chai').expect;

// var assert = require('assert'); //from Mocha

// var should = require('chai').should(); //To use should 
// require('chai').should(); // to Attach Should to objects

//Should Usage
/*should.exist
should.not.exist
should.equal
should.not.equal
should.Throw
should.not.Throw*/


describe('Target', function() {

    before(function() {
        // runs before all tests in this block
    });
    after(function() {
        // runs after all tests in this block
    });
    beforeEach(function() {
        // runs before each test in this block
    });
    afterEach(function() {
        // runs after each test
    });
    //Assertions
    it('Should Be Ok', function() {
        //{}.should.should.[what];
    });
});